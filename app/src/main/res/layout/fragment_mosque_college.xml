<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/colorPrimaryDark"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    tools:context=".ui.mosque.mosque_college.MosqueCollegeFragment">
    <androidx.appcompat.widget.Toolbar
        android:id="@+id/college_toolbar"
        style="@style/toolbar"
        android:background="@color/colorPrimaryTool"
        android:visibility="visible"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:popupTheme="@style/Theme.Menu"
        tools:ignore="MissingConstraints">
        <LinearLayout
            style="@style/add_btn">
            <ImageView
                style="@style/add_icon"/>
            <TextView
                style="@style/add_text"/>

        </LinearLayout>

        <TextView
            android:text="@string/college_tv"
            android:id="@+id/toolbar_text"
            style="@style/text_mosque_toolbar" />
    </androidx.appcompat.widget.Toolbar>

    <ProgressBar
        android:id="@+id/loading_college"
        android:indeterminateTint="@color/black"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:visibility="gone"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recycler_college"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        android:layout_marginBottom="70dp"
        app:layout_constraintTop_toBottomOf="@+id/college_toolbar"
        tools:listitem="@layout/list_college" />


</androidx.constraintlayout.widget.ConstraintLayout>