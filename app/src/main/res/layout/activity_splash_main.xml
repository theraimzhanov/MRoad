<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/green"
    tools:context=".splash.SplashMainActivity">

    <ImageView
        android:id="@+id/logo_rectangle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/ic_rectangle_splash"
        android:layout_marginBottom="200dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <ImageView
        android:id="@+id/logo_icon"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/ic_logo"
        app:layout_constraintBottom_toBottomOf="@+id/logo_rectangle"
        app:layout_constraintEnd_toEndOf="@+id/logo_rectangle"
        app:layout_constraintStart_toStartOf="@+id/logo_rectangle"
        app:layout_constraintTop_toTopOf="@+id/logo_rectangle" />

    <TextView
        android:id="@+id/tv_splash"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="10dp"
        android:textSize="20sp"
        android:fontFamily="@font/montserrat"
        android:textStyle="bold"
        android:textColor="@color/colorPrimaryLight"
        android:text="@string/muslim_road"
        app:layout_constraintEnd_toEndOf="@+id/logo_rectangle"
        app:layout_constraintStart_toStartOf="@+id/logo_rectangle"
        app:layout_constraintTop_toBottomOf="@+id/logo_rectangle" />

</androidx.constraintlayout.widget.ConstraintLayout>